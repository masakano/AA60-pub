//
//
//
#pragma once

#ifdef def_use_debug_colors
common:
#define c0 0.05
#define c1 1.00

const vec3 c_debug_colors[8] = {
        vec3(c1, c0, c0), vec3(c0, c1, c0), vec3(c0, c0, c1), vec3(c0, c1, c1),
        vec3(c1, c0, c1), vec3(c0, c0, c1), vec3(c0, c0, c0), vec3(c1, c1, c1),
#undef c0
#undef c1	
};
#endif

frag:
vec4 g_debug_color = vec4(1.0);
common:



