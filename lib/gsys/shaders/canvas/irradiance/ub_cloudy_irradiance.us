//
//
//
#pragma once
#include <default/ub_begin.h>

UNIF_NAMESPACE(spu::gs_canvas::irradiance)
UNIF_BEGIN(UB_CLOUDY_IRRADIANCE)
{
	vec3 sundir;
	float _f0;
	vec3 offset;
	float _f1;
	float sky_camera_height;
	float cloud_bottom;
	float cloud_top;
	float granularity;
	float lacunarity;
	float gain;
	float density_min;
	float density_max;
	float density_scale;
	float sky_cutoff_angle;
	float cloud_fade_angle;
	int octaves;
	int steps;
} UNIF_END(ub_cloudy_irradiance);

#include <default/ub_end.h>


